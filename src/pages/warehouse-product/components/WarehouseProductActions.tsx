"use client";

// import { Button } from "@/components/ui/button";
// import WarehouseProductModal from "./WarehouseProductModal";
// import { Plus } from "lucide-react";
// import { WAREHOUSE_PRODUCT } from "../lib/warehouse-product.interface";
// import { useState } from "react";

export default function WarehouseProductActions() {
  // const [open, setOpen] = useState(false);

  // const { MODEL } = WAREHOUSE_PRODUCT;
  return (
    <div className="flex items-center gap-2">
      {/* <Button
        
        className="ml-auto"
        onClick={() => setOpen(true)}
      >
        <Plus className="size-4 mr-2" /> Asignar Producto
      </Button>
      <WarehouseProductModal
        title={MODEL.name}
        mode="create"
        open={open}
        onClose={() => setOpen(false)}
      /> */}
    </div>
  );
}
